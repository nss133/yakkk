# 약관 조문 비교 심사 도구 (Phase 1) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 초안(docx) 조문마다 DB의 닮은 타사 조문을 자동으로 끌어와 나란히 대조하는, LLM 없이 동작하는 약관 심사 도구를 만든다.

**Architecture:** 무거운 계산은 빌드 시점(문자 n-gram IDF 산출·DB 적재), 런타임은 FTS로 후보를 좁힌 뒤 stdlib 파이썬 코사인으로 재정렬. 진입점은 `search_app.py`에 단건/붙여넣기/docx 일괄 라우트를 추가. 조문분할 로직을 `clause_split.py`로 분리해 PDF 추출기와 docx 프론트가 공유하고, 그 과정에서 제목 파싱을 개선(버그①)한다.

**Tech Stack:** Python 3.14, SQLite(FTS5), PyMuPDF(fitz, 빌드전용), python-docx(런타임 docx), pytest(테스트). 유사도 엔진·검색앱 런타임은 표준 라이브러리만.

## Global Constraints

- **런타임 유사도 엔진(`simmatch.py`)과 검색앱 핵심 경로는 표준 라이브러리만 사용** — numpy·scipy·sklearn 금지. 무거운 계산은 빌드 시점으로.
- **비교 대상은 타사만** — 결과에서 자사(미래에셋, member_cd `L34`) 제외.
- **Python 3.14 기준** — `cgi` 모듈 없음. multipart는 `email`/수동 파싱.
- **경로 기준** `~/yakgwan-db`. 스크립트는 `src/`에서 `.venv/bin/python`으로 실행(`cwd=src`). DB는 `db/terms.db`(마스터)·`db/terms_dist_current.db`(반입 현행판).
- **멱등·증분** — 빌드 스크립트는 재실행 안전해야 함.
- 코드 스타일은 기존 `src/*.py`를 따름(한글 docstring/주석, 짧은 함수, 표준 라이브러리 우선).

---

### Task 1: 개발 의존성(pytest·python-docx) + 테스트 골격

**Files:**
- Create: `tests/conftest.py` (`src/`를 sys.path에 추가 — 모듈의 bare import와 일치)
- Create: `tests/test_smoke.py`
- Modify: `.gitignore` (이미 `.venv` 무시 확인만)

**Interfaces:**
- Produces: `pytest` 실행 환경. 이후 모든 태스크가 `.venv/bin/pytest`로 테스트.
- 규약: 테스트는 모듈을 **bare import**(`import simmatch`, `from clause_split import ...`)로 불러온다 — `src/*.py`가 서로를 `import common`처럼 부르는 방식과 동일. `conftest.py`가 `src/`를 경로에 넣어준다. `from src.xxx import` 형태는 쓰지 않는다.

- [ ] **Step 1: 의존성 설치**

Run:
```bash
cd ~/yakgwan-db && .venv/bin/pip install pytest python-docx
```
Expected: `Successfully installed ... pytest-... python-docx-...`

- [ ] **Step 2: conftest + 스모크 테스트 작성**

Create `tests/conftest.py`:
```python
"""테스트에서 src/ 모듈을 bare import 할 수 있도록 경로 추가."""
import pathlib
import sys

sys.path.insert(0, str(pathlib.Path(__file__).resolve().parent.parent / "src"))
```
Create `tests/test_smoke.py`:
```python
"""테스트 환경 스모크."""
import importlib


def test_imports():
    # 빌드전용/런타임 핵심 모듈이 import 가능해야 함
    assert importlib.import_module("docx")
    assert importlib.import_module("sqlite3")
```

- [ ] **Step 3: 실행해서 통과 확인**

Run: `cd ~/yakgwan-db && .venv/bin/python -m pytest tests/test_smoke.py -v`
Expected: PASS (1 passed)

- [ ] **Step 4: Commit**

```bash
cd ~/yakgwan-db && git add tests .gitignore && \
git commit -m "test: pytest·python-docx 도입 + 테스트 골격"
```

---

### Task 2: `clause_split.py` — 조문분할 분리 + 제목 파싱 개선(버그①)

기존 `extract_index.py`의 `split_clauses`를 별도 모듈로 옮기고, 세 가지 실패 패턴을 복구한다:
(A) 다음 줄 bare 제목 `제1조`↵`목적`↵`31`  (신한·KB)
(B) 다음 줄 브래킷 제목 `제1조`↵`[목적]` / `【목적】`  (삼성·NH)
(C) 다음 줄 여러 줄 괄호 제목 `제29조`↵`(…연체되는 경우 납입최`↵`고(독촉)와 계약의 해지)`  (NH)
같은 줄 괄호 제목(교보·미래·한화)은 기존 동작 유지.

**Files:**
- Create: `src/clause_split.py`
- Create: `tests/test_clause_split.py`
- Modify: `src/extract_index.py:34-81` (분할 로직 제거하고 `clause_split`에서 import)

**Interfaces:**
- Produces:
  - `clause_split.split_clauses(text: str) -> list[tuple[str|None, str, str]]` — `(clause_no, title, body)` 순차 청크. `clause_no`는 `제3조`/`제3조의2`/`별표4`/`None`.
  - `clause_split.norm_no(s: str) -> str`
- Consumes: 없음(순수 함수).

- [ ] **Step 1: 실패 테스트 작성 (실데이터 패턴)**

Create `tests/test_clause_split.py`:
```python
from clause_split import split_clauses


def _titles(text):
    return {no: ti for no, ti, _ in split_clauses(text) if no}


def test_inline_paren_title_kept():   # 교보·미래·한화 (기존 동작)
    t = "제3조 (보험금의 지급) 회사는 보험금을 지급합니다.\n다음 각 호를 따릅니다."
    out = split_clauses(t)
    no, ti, body = [c for c in out if c[0] == "제3조"][0]
    assert ti == "보험금의 지급"
    assert body.startswith("회사는 보험금을 지급합니다.")


def test_next_line_bare_title():   # 신한·KB: 제1조 / 목적 / 31
    t = "제1조\n목적\n31\n이 약관은 목적을 정합니다.\n제2조\n용어의 정의\n31"
    assert _titles(t)["제1조"] == "목적"
    assert _titles(t)["제2조"] == "용어의 정의"


def test_next_line_bracket_title():   # 삼성 [목적], NH 【목적】
    assert _titles("제1조\n[목적]\n6\n본문")["제1조"] == "목적"
    assert _titles("제1조\n【목적】\n39\n본문")["제1조"] == "목적"


def test_next_line_multiline_paren_title():   # NH 여러 줄 괄호
    t = ("제29조\n(보험료의 납입이 연체되는 경우 납입최\n고(독촉)와 계약의 해지)\n"
         "P.70\n계약자는 …")
    ti = _titles(t)["제29조"]
    assert "계약의 해지" in ti and ti.startswith("보험료의 납입")


def test_byulpyo_still_works():
    out = split_clauses("[별표 4] 장해분류표\n내용")
    assert any(no == "별표4" and "장해분류표" in ti for no, ti, _ in out)


def test_body_line_not_stolen_as_title():   # 오검출 방지: 문장은 제목 아님
    t = "제5조\n계약자는 보험료를 납입하여야 합니다.\n제6조"
    assert _titles(t)["제5조"] == ""   # 문장은 제목으로 안 잡힘
```

- [ ] **Step 2: 실행해서 실패 확인**

Run: `cd ~/yakgwan-db && .venv/bin/python -m pytest tests/test_clause_split.py -v`
Expected: FAIL (`ModuleNotFoundError: src.clause_split`)

- [ ] **Step 3: `clause_split.py` 구현**

Create `src/clause_split.py`:
```python
#!/usr/bin/env python3
"""조문 분할 + 제목 파싱(회사별 레이아웃 대응). extract_index·docx_split 공용.

지원 제목 패턴:
- 같은 줄 괄호:  제3조 (보험금의 지급) 본문…
- 다음 줄 bare:  제1조 / 목적 / 31
- 다음 줄 브래킷: 제1조 / [목적] 또는 【목적】
- 다음 줄 여러 줄 괄호: 제29조 / (…납입최 / 고…해지)
"""
import re

RE_JO = re.compile(r"^\s*(제\s*\d+\s*조(?:\s*의\s*\d+)?)\s*(.*)$")
RE_BYULPYO = re.compile(r"^\s*[\[(【]?\s*(별\s*표\s*\d*)\s*[\])】]?\s*(.{0,60})$")
RE_PAGE = re.compile(r"^\s*(?:P\.?\s*)?\d{1,4}\s*$")            # 페이지번호/숫자만 행
RE_MARKER = re.compile(r"^\s*[①-⓿❶-❿]+\s*$")  # 원문자 마커 행

_OPEN = {"(": ")", "[": "]", "【": "】", "〔": "〕"}
_CLOSE = set(")]】〕")
_STRIP = "[]()【】〔〕 \t"


def norm_no(s: str) -> str:
    return re.sub(r"\s+", "", s)


def _clean_title(s: str) -> str:
    return re.sub(r"\s+", " ", s.strip(_STRIP)).strip()


def _looks_title(s: str) -> bool:
    """짧은 한글 어구이며 본문 문장이 아닌 것만 제목 후보로."""
    s = s.strip()
    if not (0 < len(s) <= 40):
        return False
    if RE_JO.match(s) or RE_PAGE.match(s) or RE_MARKER.match(s):
        return False
    if s[0] in "①②③④⑤⑥⑦⑧⑨⑩0123456789":
        return False
    if s.endswith(("니다.", "니다", "습니다", "합니다")):   # 서술 문장 배제
        return False
    return bool(re.search(r"[가-힣]", s))


def _inline_title(rest: str):
    """같은 줄 '(제목) 본문' → (title, body). 괄호로 안 시작하면 ('', rest)."""
    rest = rest.strip()
    if rest and rest[0] in _OPEN:
        depth, end = 0, -1
        for idx, ch in enumerate(rest):
            if ch in _OPEN:
                depth += 1
            elif ch in _CLOSE:
                depth -= 1
                if depth == 0:
                    end = idx
                    break
        if end != -1:
            title = _clean_title(rest[:end + 1])
            if 0 < len(title) <= 60:
                return title, rest[end + 1:].strip()
    return "", rest


def _bracket_title(lines, i):
    """lines[i]가 여는 괄호로 시작하면 닫힐 때까지(최대 5행) 모아 (title, next_i)."""
    if i >= len(lines):
        return None, i
    s = lines[i].strip()
    if not s or s[0] not in _OPEN:
        return None, i
    depth, buf, j = 0, [], i
    while j < len(lines) and j < i + 5:
        seg = lines[j].strip()
        for ch in seg:
            if ch in _OPEN:
                depth += 1
            elif ch in _CLOSE:
                depth -= 1
        buf.append(seg)
        j += 1
        if depth <= 0:
            break
    title = _clean_title(" ".join(buf))
    return (title, j) if 0 < len(title) <= 60 else (None, i)


def split_clauses(text: str):
    """문서 전체를 순차 청크로 분할. 반환: [(clause_no|None, title, body)]"""
    lines = text.split("\n")
    chunks = []
    cur_no, cur_title, cur_lines = None, "", []

    def flush():
        nonlocal cur_no, cur_title, cur_lines
        body = "\n".join(cur_lines).strip()
        if body or cur_no:
            chunks.append((cur_no, cur_title, body))
        cur_no, cur_title, cur_lines = None, "", []

    i, n = 0, len(lines)
    while i < n:
        ln = lines[i]
        m = RE_JO.match(ln)
        if m:
            flush()
            cur_no = norm_no(m.group(1))
            rest = (m.group(2) or "").strip()
            title, body_rest = _inline_title(rest)
            if title:
                cur_title, cur_lines, i = title, ([body_rest] if body_rest else []), i + 1
                continue
            # 같은 줄에 제목 없음 → 다음 줄에서 복구
            cur_lines = [rest] if rest else []
            j = i + 1
            while j < n and not lines[j].strip():
                j += 1
            bt, nj = _bracket_title(lines, j)
            if bt:
                cur_title, i = bt, nj
                continue
            if j < n:
                cand = lines[j].strip()
                nxt = lines[j + 1].strip() if j + 1 < n else ""
                if _looks_title(cand) and (
                    not nxt or RE_PAGE.match(nxt) or RE_MARKER.match(nxt)
                    or nxt.startswith("제")
                ):
                    cur_title, i = cand, j + 1
                    continue
            i += 1
            continue
        mb = RE_BYULPYO.match(ln)
        if mb and len(ln.strip()) < 70:
            flush()
            cur_no, cur_title, cur_lines, i = norm_no(mb.group(1)), mb.group(2).strip(), [], i + 1
            continue
        cur_lines.append(ln)
        i += 1
    flush()

    out = []
    for k, (no, title, body) in enumerate(chunks):
        if no is None:
            title = title or ("[전문]" if k == 0 else "[본문외]")
        out.append((no, title, body))
    return out
```

- [ ] **Step 4: `extract_index.py`가 `clause_split`을 쓰도록 수정**

`src/extract_index.py`에서 `RE_JO`/`RE_BYULPYO`/`norm_no`/`split_clauses` 정의(34~81행)를 삭제하고, import 부(18행 `from common import ROOT, open_db` 아래)에 추가:
```python
from clause_split import split_clauses
```
(`import re`는 `extract_pdf_text`에서 계속 쓰므로 유지.)

- [ ] **Step 5: 실행해서 통과 확인**

Run: `cd ~/yakgwan-db && .venv/bin/python -m pytest tests/test_clause_split.py -v && .venv/bin/python -c "import sys; sys.path.insert(0,'src'); import extract_index; print('extract_index import OK')"`
Expected: PASS (6 passed) + `extract_index import OK`

- [ ] **Step 6: Commit**

```bash
cd ~/yakgwan-db && git add src/clause_split.py src/extract_index.py tests/test_clause_split.py && \
git commit -m "feat: 조문분할 분리 + 다음줄/브래킷/멀티라인 제목 파싱(버그①)"
```

---

### Task 3: 재추출 옵션 + 제목 채움률 개선 검증

개선된 파서로 기존 문서를 재추출하고, title 공란율이 실제로 떨어지는지 측정한다.
`extract_index.py`는 "이미 clauses 있는 doc_id 스킵"이라, 재추출하려면 clauses를 비워야 함.

**Files:**
- Modify: `src/extract_index.py` (main에 `--reextract` 플래그: 대상 doc의 clauses 삭제 후 재적재)

**Interfaces:**
- Consumes: `clause_split.split_clauses` (Task 2)
- Produces: 재추출된 `clauses.title`. 이후 build_simindex·export가 이를 사용.

- [ ] **Step 1: `--reextract` 플래그 추가**

`src/extract_index.py`의 `main()`에서 argparse에 추가(99행 `--doc-types` 아래):
```python
    ap.add_argument("--reextract", action="store_true",
                    help="대상 문서의 기존 clauses를 지우고 다시 추출")
```
그리고 `done_ids` 계산부(114행) 앞에 삽입:
```python
    if args.reextract:
        doc_ids = [d[0] for d in docs]
        conn.executemany("DELETE FROM clauses WHERE doc_id=?", [(x,) for x in doc_ids])
        conn.commit()
        print(f"재추출: {len(doc_ids)}개 문서 clauses 삭제")
```

- [ ] **Step 2: 개선 전 공란율 스냅샷**

Run:
```bash
cd ~/yakgwan-db && .venv/bin/python - <<'PY'
import sqlite3
c=sqlite3.connect("db/terms.db")
q="""SELECT i.name, ROUND(100.0*SUM(CASE WHEN c.title IS NULL OR c.title='' THEN 1 END)/COUNT(*),1)
     FROM clauses c JOIN documents d USING(doc_id) JOIN insurers i ON i.member_cd=d.member_cd
     WHERE c.clause_no LIKE '제%조%' GROUP BY i.name ORDER BY 2 DESC"""
for r in c.execute(q): print(f"{r[0]:12} {r[1]}%")
PY
```
Expected: 신한·NH·KB 등 80~90%대 출력(개선 전 기준선 기록).

- [ ] **Step 3: 전체 재추출 실행**

Run: `cd ~/yakgwan-db/src && ../.venv/bin/python extract_index.py --reextract`
Expected: `완료: 성공 N, 실패 0 | 총 조항청크 …` (실패 0 확인)

- [ ] **Step 4: 개선 후 공란율 재측정(검증 게이트)**

Step 2와 동일 쿼리 재실행.
Expected: 신한·KB·NH·삼성의 `제N조` 제목 공란율이 **뚜렷이 하락**(목표: 신한/KB/NH 80%대 → 30% 이하). 흥국은 PDF 추출 뒤엉킴으로 부분 개선만 될 수 있음(허용). 하락이 없으면 Task 2로 돌아가 표본 재확인.

- [ ] **Step 5: Commit**

```bash
cd ~/yakgwan-db && git add src/extract_index.py && \
git commit -m "feat: extract_index --reextract + 제목 재추출로 공란율 개선"
```

---

### Task 4: `simmatch.py` — 정규화·n-gram·코사인 (순수 함수)

**Files:**
- Create: `src/simmatch.py`
- Create: `tests/test_simmatch.py`

**Interfaces:**
- Produces (stdlib 전용):
  - `simmatch.normalize(text: str) -> str`
  - `simmatch.char_ngrams(text: str, sizes=(3,4)) -> list[str]`
  - `simmatch.vectorize(text: str, idf: dict[str,float], default_idf: float) -> dict[str,float]` (L2 정규화)
  - `simmatch.cosine(v1: dict, v2: dict) -> float`

- [ ] **Step 1: 실패 테스트 작성**

Create `tests/test_simmatch.py`:
```python
import math

import simmatch


def test_normalize_strips_space_and_punct():
    assert simmatch.normalize("보험금의  지급(사유)!") == "보험금의지급사유"


def test_char_ngrams_sizes():
    g = simmatch.char_ngrams("보험금지", sizes=(3,))
    assert g == ["보험금", "험금지"]


def test_cosine_identical_is_one():
    idf = {}
    v = simmatch.vectorize("보험금의 지급사유", idf, default_idf=1.0)
    assert abs(simmatch.cosine(v, v) - 1.0) < 1e-9


def test_cosine_similar_higher_than_dissimilar():
    idf, d = {}, 1.0
    q = simmatch.vectorize("피보험자가 사망한 경우 보험금을 지급합니다", idf, d)
    near = simmatch.vectorize("피보험자가 사망한 때에 보험금을 지급함", idf, d)
    far = simmatch.vectorize("보험료의 납입을 연체하면 계약이 해지됩니다", idf, d)
    assert simmatch.cosine(q, near) > simmatch.cosine(q, far)


def test_idf_weights_rare_grams_more():
    q = simmatch.vectorize("가나다라", {"가나다": 5.0, "나다라": 1.0}, default_idf=1.0)
    assert q["가나다"] > q["나다라"]
```

- [ ] **Step 2: 실행해서 실패 확인**

Run: `cd ~/yakgwan-db && .venv/bin/python -m pytest tests/test_simmatch.py -v`
Expected: FAIL (`ModuleNotFoundError: src.simmatch`)

- [ ] **Step 3: `simmatch.py` 코어 구현**

Create `src/simmatch.py`:
```python
#!/usr/bin/env python3
"""문자 n-gram TF-IDF 코사인 유사도 (표준 라이브러리 전용).

빌드 시점 산출 IDF(ngram_idf 테이블)를 dict로 받아, 런타임에 조문 두 개의
코사인을 계산한다. FTS 후보 검색은 db_similar()에서 결합(Task 6).
"""
import math
import re
import unicodedata
from collections import Counter

_KEEP = re.compile(r"[^가-힣a-z0-9]")


def normalize(text: str) -> str:
    t = unicodedata.normalize("NFC", text or "").lower()
    return _KEEP.sub("", t)


def char_ngrams(text: str, sizes=(3, 4)):
    t = normalize(text)
    grams = []
    for nsz in sizes:
        if len(t) >= nsz:
            grams.extend(t[k:k + nsz] for k in range(len(t) - nsz + 1))
    return grams


def vectorize(text: str, idf: dict, default_idf: float) -> dict:
    tf = Counter(char_ngrams(text))
    vec = {g: (1.0 + math.log(c)) * idf.get(g, default_idf) for g, c in tf.items()}
    norm = math.sqrt(sum(w * w for w in vec.values())) or 1.0
    return {g: w / norm for g, w in vec.items()}


def cosine(v1: dict, v2: dict) -> float:
    if len(v1) > len(v2):
        v1, v2 = v2, v1
    return sum(w * v2.get(g, 0.0) for g, w in v1.items())
```

- [ ] **Step 4: 실행해서 통과 확인**

Run: `cd ~/yakgwan-db && .venv/bin/python -m pytest tests/test_simmatch.py -v`
Expected: PASS (5 passed)

- [ ] **Step 5: Commit**

```bash
cd ~/yakgwan-db && git add src/simmatch.py tests/test_simmatch.py && \
git commit -m "feat: simmatch 코어(정규화·n-gram·TF-IDF 코사인, stdlib)"
```

---

### Task 5: `build_simindex.py` — ngram_idf 테이블 생성

**Files:**
- Create: `src/build_simindex.py`
- Create: `tests/test_build_simindex.py`

**Interfaces:**
- Consumes: `simmatch.char_ngrams` (Task 4)
- Produces:
  - 테이블 `ngram_idf(ngram TEXT PRIMARY KEY, idf REAL)` in `db/terms.db`
  - 테이블 `simindex_meta(key TEXT PRIMARY KEY, value TEXT)` — `n_docs`, `default_idf`
  - 함수 `build_simindex.build(conn, df_floor=3, df_ceil_ratio=0.4)` (테스트용 주입 가능)

- [ ] **Step 1: 실패 테스트 작성**

Create `tests/test_build_simindex.py`:
```python
import sqlite3

import build_simindex


def _mk_db():
    c = sqlite3.connect(":memory:")
    c.executescript("""
        CREATE TABLE clauses(clause_id INTEGER PRIMARY KEY, text TEXT);
        INSERT INTO clauses(text) VALUES
          ('피보험자가 사망한 경우 보험금을 지급합니다 사망보험금 지급기준'),
          ('피보험자가 사망한 때 사망보험금을 지급합니다 지급기준 명시'),
          ('보험료의 납입을 연체하면 계약이 해지됩니다 납입최고 독촉');
    """)
    c.commit()
    return c


def test_build_creates_idf_and_meta():
    c = _mk_db()
    build_simindex.build(c, df_floor=1, df_ceil_ratio=1.0)
    n = c.execute("SELECT COUNT(*) FROM ngram_idf").fetchone()[0]
    assert n > 0
    meta = dict(c.execute("SELECT key, value FROM simindex_meta"))
    assert meta["n_docs"] == "3"
    assert float(meta["default_idf"]) > 0


def test_common_gram_has_lower_idf_than_rare():
    c = _mk_db()
    build_simindex.build(c, df_floor=1, df_ceil_ratio=1.0)
    idf = dict(c.execute("SELECT ngram, idf FROM ngram_idf"))
    # '보험금'은 2개 문서(사망 관련), '납입최'는 1개 문서 → 후자가 더 희귀(높은 idf)
    if "보험금" in idf and "납입최" in idf:
        assert idf["납입최"] > idf["보험금"]
```

- [ ] **Step 2: 실행해서 실패 확인**

Run: `cd ~/yakgwan-db && .venv/bin/python -m pytest tests/test_build_simindex.py -v`
Expected: FAIL (`ModuleNotFoundError: src.build_simindex`)

- [ ] **Step 3: `build_simindex.py` 구현**

Create `src/build_simindex.py`:
```python
#!/usr/bin/env python3
"""문자 n-gram IDF를 코퍼스에서 산출해 ngram_idf 테이블에 적재(빌드 시점).

- 대상: clauses.text 중 length>=30 (FTS 인덱싱 대상과 동일)
- df_floor 미만(희귀) / df_ceil_ratio 초과(너무 흔함) n-gram은 제외
- 미등재 n-gram의 런타임 기본 idf(default_idf)를 simindex_meta에 저장
사용법: .venv/bin/python src/build_simindex.py
"""
import argparse
import math
from collections import defaultdict

from common import open_db
from simmatch import char_ngrams

SCHEMA = """
CREATE TABLE IF NOT EXISTS ngram_idf(ngram TEXT PRIMARY KEY, idf REAL);
CREATE TABLE IF NOT EXISTS simindex_meta(key TEXT PRIMARY KEY, value TEXT);
"""


def build(conn, df_floor: int = 3, df_ceil_ratio: float = 0.4):
    conn.executescript(SCHEMA)
    conn.execute("DELETE FROM ngram_idf")
    conn.execute("DELETE FROM simindex_meta")

    rows = conn.execute("SELECT text FROM clauses WHERE length(text) >= 30")
    df = defaultdict(int)
    n_docs = 0
    for (text,) in rows:
        n_docs += 1
        for g in set(char_ngrams(text)):
            df[g] += 1
    if n_docs == 0:
        raise SystemExit("clauses가 비어 있음 — 먼저 extract_index 실행")

    ceil = max(df_floor, int(n_docs * df_ceil_ratio))
    kept = [(g, math.log((n_docs + 1) / (d + 1)) + 1.0)
            for g, d in df.items() if df_floor <= d <= ceil]
    conn.executemany("INSERT OR REPLACE INTO ngram_idf(ngram, idf) VALUES(?,?)", kept)

    default_idf = math.log((n_docs + 1) / (max(1, df_floor - 1) + 1)) + 1.0
    conn.executemany("INSERT OR REPLACE INTO simindex_meta(key, value) VALUES(?,?)",
                     [("n_docs", str(n_docs)), ("default_idf", f"{default_idf:.6f}")])
    conn.commit()
    return len(kept), n_docs


def main():
    ap = argparse.ArgumentParser()
    ap.add_argument("--df-floor", type=int, default=3)
    ap.add_argument("--df-ceil-ratio", type=float, default=0.4)
    args = ap.parse_args()
    conn = open_db()
    n_kept, n_docs = build(conn, args.df_floor, args.df_ceil_ratio)
    size = conn.execute("SELECT COUNT(*) FROM ngram_idf").fetchone()[0]
    conn.close()
    print(f"완료: n-gram {n_kept:,}개 적재 (문서 {n_docs:,}) — ngram_idf {size:,}행")


if __name__ == "__main__":
    main()
```
> 테스트는 `build(conn, ...)`를 직접 호출(메모리 DB 주입). `main()`은 `open_db()` 경로.

- [ ] **Step 4: 실행해서 통과 확인**

Run: `cd ~/yakgwan-db && .venv/bin/python -m pytest tests/test_build_simindex.py -v`
Expected: PASS (2 passed)

- [ ] **Step 5: 실 DB 빌드 + 크기 측정(튜닝 게이트)**

Run: `cd ~/yakgwan-db/src && ../.venv/bin/python build_simindex.py`
Expected: `완료: n-gram N개 적재 …`. 이어서 테이블 용량 확인:
```bash
cd ~/yakgwan-db && .venv/bin/python -c "import sqlite3;c=sqlite3.connect('db/terms.db');print('rows',c.execute('SELECT COUNT(*) FROM ngram_idf').fetchone()[0])"
```
`rows`가 과다(수백만)하면 `--df-floor 4` 등으로 재실행해 반입 DB 증가폭을 억제(설계 목표: 현행판 +30% 이내). 적정하면 다음 태스크로.

- [ ] **Step 6: Commit**

```bash
cd ~/yakgwan-db && git add src/build_simindex.py tests/test_build_simindex.py && \
git commit -m "feat: build_simindex — 문자 n-gram IDF 적재(ngram_idf)"
```

---

### Task 6: `db_similar()` — FTS 후보 + 코사인 재정렬 (DB 통합)

**Files:**
- Modify: `src/simmatch.py` (DB 연동 함수 추가)
- Create: `tests/test_db_similar.py`

**Interfaces:**
- Consumes: `ngram_idf`/`simindex_meta`(Task 5), `clauses_fts`(export_dist), `simmatch.vectorize/cosine`(Task 4)
- Produces:
  - `simmatch.load_idf(conn) -> tuple[dict, float]` — `(idf, default_idf)`
  - `simmatch.fts_query(text: str, max_terms=15) -> str`
  - `simmatch.db_similar(conn, query_text, idf, default_idf, top_n=10, exclude_member=None, prod_group=None, query_title=None, cand_limit=300) -> list[dict]`
    반환 각 항목 키: `score, clause_id, clause_no, title, doc_id, member_cd, insurer, prod_nm_raw, version_label, prod_group, text`

- [ ] **Step 1: 실패 테스트 작성**

Create `tests/test_db_similar.py`:
```python
import sqlite3

import build_simindex
import simmatch


def _fixture():
    c = sqlite3.connect(":memory:")
    c.executescript("""
      CREATE TABLE insurers(member_cd TEXT PRIMARY KEY, name TEXT);
      CREATE TABLE documents(doc_id INTEGER PRIMARY KEY, member_cd TEXT, prod_nm_raw TEXT,
                             version_label TEXT, prod_group TEXT);
      CREATE TABLE clauses(clause_id INTEGER PRIMARY KEY, doc_id INTEGER,
                           clause_no TEXT, title TEXT, text TEXT);
      INSERT INTO insurers VALUES ('L34','미래에셋'),('L03','삼성'),('L01','한화');
      INSERT INTO documents VALUES
        (1,'L34','자사종신','2024~','종신'),
        (2,'L03','삼성종신','2024~','종신'),
        (3,'L01','한화종신','2024~','종신');
      INSERT INTO clauses VALUES
        (10,1,'제3조','보험금의 지급','피보험자가 사망한 경우 회사는 사망보험금을 지급합니다 지급사유'),
        (20,2,'제3조','보험금의 지급','피보험자가 사망한 때에 회사는 사망보험금을 지급함 지급사유 명시'),
        (30,3,'제9조','보험료 납입','보험료의 납입을 연체하면 납입최고 후 계약이 해지됩니다');
      CREATE VIRTUAL TABLE clauses_fts USING fts5(text, title, content='clauses', content_rowid='clause_id');
      INSERT INTO clauses_fts(rowid,text,title)
        SELECT clause_id,text,COALESCE(title,'') FROM clauses WHERE length(text)>=30;
    """)
    c.row_factory = sqlite3.Row
    build_simindex.build(c, df_floor=1, df_ceil_ratio=1.0)
    return c


def test_db_similar_ranks_same_topic_first_and_excludes_self():
    c = _fixture()
    idf, d = simmatch.load_idf(c)
    q = "피보험자가 사망한 경우 회사는 사망보험금을 지급합니다"
    res = simmatch.db_similar(c, q, idf, d, top_n=5, exclude_member="L34")
    assert res, "결과가 있어야 함"
    assert all(r["member_cd"] != "L34" for r in res)     # 자사 제외
    assert res[0]["member_cd"] == "L03"                   # 같은 취지(삼성)가 최상위
    assert res[0]["score"] > 0


def test_fts_query_builds_or_terms():
    q = simmatch.fts_query("보험금의 지급 사유")
    assert "OR" in q and '"보험금의"' in q
```

- [ ] **Step 2: 실행해서 실패 확인**

Run: `cd ~/yakgwan-db && .venv/bin/python -m pytest tests/test_db_similar.py -v`
Expected: FAIL (`AttributeError: module 'src.simmatch' has no attribute 'load_idf'`)

- [ ] **Step 3: `simmatch.py`에 DB 연동 추가**

`src/simmatch.py` 끝에 추가:
```python
def load_idf(conn):
    """ngram_idf/simindex_meta → (idf dict, default_idf). 앱 시작 시 1회 로드."""
    idf = {g: v for g, v in conn.execute("SELECT ngram, idf FROM ngram_idf")}
    meta = dict(conn.execute("SELECT key, value FROM simindex_meta"))
    default_idf = float(meta.get("default_idf", 1.0))
    return idf, default_idf


def fts_query(text: str, max_terms: int = 15) -> str:
    """조문에서 변별력 있는 어절 상위 N개를 OR로 결합한 FTS5 질의."""
    toks = re.findall(r"[가-힣a-z0-9]{2,}", (text or "").lower())
    seen, terms = set(), []
    for t in sorted(toks, key=len, reverse=True):
        if t not in seen:
            seen.add(t)
            terms.append(t)
        if len(terms) >= max_terms:
            break
    return " OR ".join(f'"{t}"' for t in terms)


_SQL = """SELECT c.clause_id, c.clause_no, c.title, c.text, d.doc_id, d.member_cd,
                 d.prod_nm_raw, d.version_label, d.prod_group, i.name AS insurer
          FROM clauses_fts f JOIN clauses c ON c.clause_id=f.rowid
          JOIN documents d USING(doc_id) JOIN insurers i ON i.member_cd=d.member_cd
          WHERE clauses_fts MATCH ?"""


def db_similar(conn, query_text, idf, default_idf, top_n=10,
               exclude_member=None, prod_group=None, query_title=None, cand_limit=300):
    fq = fts_query(query_text)
    if not fq:
        return []
    sql, params = _SQL, [fq]
    if exclude_member:
        sql += " AND d.member_cd<>?"
        params.append(exclude_member)
    if prod_group:
        sql += " AND d.prod_group=?"
        params.append(prod_group)
    sql += " ORDER BY rank LIMIT ?"
    params.append(cand_limit)

    qv = vectorize(query_text, idf, default_idf)
    qtitle = normalize(query_title) if query_title else ""
    out = []
    for r in conn.execute(sql, params):
        score = cosine(qv, vectorize(r["text"], idf, default_idf))
        if qtitle and normalize(r["title"] or "") == qtitle:
            score += 0.05           # 조문제목 일치 가산점
        out.append({"score": round(score, 4), **{k: r[k] for k in r.keys()}})
    out.sort(key=lambda x: x["score"], reverse=True)
    return out[:top_n]
```
> `db_similar`은 `conn.row_factory = sqlite3.Row`를 전제(검색앱·테스트 모두 Row 사용).

- [ ] **Step 4: 실행해서 통과 확인**

Run: `cd ~/yakgwan-db && .venv/bin/python -m pytest tests/test_db_similar.py -v`
Expected: PASS (2 passed)

- [ ] **Step 5: Commit**

```bash
cd ~/yakgwan-db && git add src/simmatch.py tests/test_db_similar.py && \
git commit -m "feat: db_similar — FTS 후보 + 코사인 재정렬(자사 제외·제목 가산점)"
```

---

### Task 7: `export_dist.py` — ngram_idf/simindex_meta 반입 포함

**Files:**
- Modify: `src/export_dist.py:28-44` (복사 테이블 목록에 두 테이블 추가)
- Create: `tests/test_export_carries_idf.py`

**Interfaces:**
- Consumes: `ngram_idf`/`simindex_meta`(Task 5)
- Produces: 반입 DB(`terms_dist*.db`)에 두 테이블 포함 → 앱이 `load_idf`로 사용.

- [ ] **Step 1: 실패 테스트 작성**

Create `tests/test_export_carries_idf.py`:
```python
import export_dist


def test_copy_list_includes_idf_tables():
    # export_dist가 두 테이블을 반입 대상에 포함하는지(소스에 명시)
    import inspect
    src = inspect.getsource(export_dist.main)
    assert "ngram_idf" in src and "simindex_meta" in src
```

- [ ] **Step 2: 실행해서 실패 확인**

Run: `cd ~/yakgwan-db && .venv/bin/python -m pytest tests/test_export_carries_idf.py -v`
Expected: FAIL (assert)

- [ ] **Step 3: `export_dist.py` 수정**

`src/export_dist.py` 28행의 테이블 튜플을 확장:
```python
    for tbl in ("insurers", "products", "documents", "clauses", "product_doc_map",
                "ngram_idf", "simindex_meta"):
```
`ngram_idf`/`simindex_meta`는 doc_id 의존이 없으므로 `current_only`에서도 전량 복사(기존 `else` 분기가 `INSERT INTO dist.{tbl} SELECT * FROM main.{tbl}`로 처리 → 추가 코드 불필요).
단, 소스 DB에 두 테이블이 없을 때(빌드 순서 문제) 대비해 33행 `src.execute(ddl...)` 앞에 가드 추가:
```python
        ddl_row = src.execute(
            "SELECT sql FROM sqlite_master WHERE type='table' AND name=?", (tbl,)).fetchone()
        if ddl_row is None:
            print(f"  (건너뜀: {tbl} 없음 — build_simindex 먼저 실행 권장)")
            continue
        ddl = ddl_row[0]
```
(기존 `ddl = src.execute(...).fetchone()[0]` 한 줄을 위 3줄 가드로 교체.)

- [ ] **Step 4: 실행해서 통과 확인 + 실 반입 재생성**

Run:
```bash
cd ~/yakgwan-db && .venv/bin/python -m pytest tests/test_export_carries_idf.py -v && \
cd src && ../.venv/bin/python export_dist.py --current-only
```
Expected: PASS + `생성: …/terms_dist_current.db`. 이어서 반입 DB에 idf 포함·크기 확인:
```bash
cd ~/yakgwan-db && .venv/bin/python -c "import sqlite3,os;c=sqlite3.connect('db/terms_dist_current.db');print('idf rows',c.execute('SELECT COUNT(*) FROM ngram_idf').fetchone()[0]);print('MB',round(os.path.getsize('db/terms_dist_current.db')/1048576,1))"
```
Expected: `idf rows > 0`, 크기가 설계 목표(현행판 대비 +30% 이내) 범위. 초과 시 Task 5 `--df-floor` 상향 후 재빌드.

- [ ] **Step 5: Commit**

```bash
cd ~/yakgwan-db && git add src/export_dist.py tests/test_export_carries_idf.py && \
git commit -m "feat: 반입 DB에 ngram_idf/simindex_meta 포함"
```

---

### Task 8: `search_app.py` — 검색조건 초기화 버튼(버그②)

**Files:**
- Modify: `src/search_app.py:34-42` (폼에 초기화 버튼), `:19-44`(PAGE 상수)
- Create: `tests/test_search_app_reset.py`

**Interfaces:**
- Produces: 폼에 `/`로 이동하는 "초기화" 버튼. 이후 Task 9·10이 같은 앱을 확장.

- [ ] **Step 1: 실패 테스트 작성**

Create `tests/test_search_app_reset.py`:
```python
import search_app


def test_page_has_reset_button():
    assert 'href="/"' in search_app.PAGE
    assert "초기화" in search_app.PAGE
```

- [ ] **Step 2: 실행해서 실패 확인**

Run: `cd ~/yakgwan-db && .venv/bin/python -m pytest tests/test_search_app_reset.py -v`
Expected: FAIL (assert)

- [ ] **Step 3: 폼에 초기화 버튼 추가**

`src/search_app.py`의 PAGE 상수에서 검색 버튼(41행 `<button>검색</button>`) 다음에 추가:
```python
 <button>검색</button>
 <a href="/" style="margin-left:8px;padding:8px;border:1px solid #ccc;border-radius:4px;color:#555">초기화</a>
```

- [ ] **Step 4: 실행해서 통과 확인**

Run: `cd ~/yakgwan-db && .venv/bin/python -m pytest tests/test_search_app_reset.py -v`
Expected: PASS (1 passed)

- [ ] **Step 5: Commit**

```bash
cd ~/yakgwan-db && git add src/search_app.py tests/test_search_app_reset.py && \
git commit -m "feat: 검색조건 초기화 버튼(버그②)"
```

---

### Task 9: `search_app.py` — 단건/붙여넣기 유사조문 라우트

**Files:**
- Modify: `src/search_app.py` (App에 `idf`/`default_idf` 클래스 속성·로드, `/similar`·`/similar_text` 라우트, do_POST, 결과 렌더 헬퍼; main에서 idf 로드)
- Create: `tests/test_similar_render.py`

**Interfaces:**
- Consumes: `simmatch.load_idf`, `simmatch.db_similar` (Task 6)
- Produces: `App.render_similar(rows: list[dict]) -> str` (회사별 그룹 HTML). `/similar?id=`·`/similar_text`.

- [ ] **Step 1: 실패 테스트 작성**

Create `tests/test_similar_render.py`:
```python
from search_app import App


def test_render_similar_groups_by_insurer():
    rows = [
        {"score": 0.81, "insurer": "삼성생명", "prod_nm_raw": "삼성종신", "clause_id": 20,
         "clause_no": "제3조", "title": "보험금의 지급", "version_label": "2024~",
         "prod_group": "종신", "text": "…"},
        {"score": 0.77, "insurer": "한화생명", "prod_nm_raw": "한화종신", "clause_id": 30,
         "clause_no": "제3조", "title": "보험금의 지급", "version_label": "2024~",
         "prod_group": "종신", "text": "…"},
    ]
    html = App.render_similar(rows)
    assert "삼성생명" in html and "한화생명" in html
    assert "81" in html          # 유사도 % 표기
    assert "/clause?id=20" in html


def test_render_similar_empty():
    assert "없" in App.render_similar([])
```

- [ ] **Step 2: 실행해서 실패 확인**

Run: `cd ~/yakgwan-db && .venv/bin/python -m pytest tests/test_similar_render.py -v`
Expected: FAIL (`AttributeError: type object 'App' has no attribute 'render_similar'`)

- [ ] **Step 3: 검색앱에 유사조문 기능 추가**

`src/search_app.py` 상단 import에 추가:
```python
import simmatch
```
`class App` 본문(56행 `db_path = None` 옆)에 클래스 속성 추가:
```python
    db_path = None
    idf = {}
    default_idf = 1.0
    self_member = "L34"   # 자사(미래에셋) — 유사비교에서 제외
```
`render_similar`를 `@staticmethod`로 App에 추가:
```python
    @staticmethod
    def render_similar(rows):
        if not rows:
            return "<p>유사한 타사 조문이 없음</p>"
        by = {}
        for r in rows:
            by.setdefault(r["insurer"], []).append(r)
        out = []
        for insurer, items in by.items():
            out.append(f"<h3>{html.escape(insurer)}</h3><table>"
                       "<tr><th>유사도</th><th>상품(판매기간)</th><th>조문</th><th>내용</th></tr>")
            for r in items:
                pct = int(round(r["score"] * 100))
                out.append(
                    f"<tr><td><b>{pct}%</b></td>"
                    f"<td><a href='/doc?id={r['doc_id']}'>{html.escape(r['prod_nm_raw'])}</a>"
                    f"<div class='meta'>{html.escape(r['version_label'] or '')}</div></td>"
                    f"<td><a href='/clause?id={r['clause_id']}'>{html.escape(r['clause_no'] or '')} "
                    f"{html.escape(r['title'] or '')}</a></td>"
                    f"<td class='meta'>{html.escape((r['text'] or '')[:120])}…</td></tr>")
            out.append("</table>")
        return "".join(out)
```
`do_GET`의 라우팅(95~100행)에 분기 추가:
```python
            if u.path == "/clause":
                self.view_clause(int(qs["id"]))
            elif u.path == "/doc":
                self.view_doc(int(qs["id"]))
            elif u.path == "/similar":
                self.similar_by_clause(int(qs["id"]))
            elif u.path == "/similar_text":
                self.similar_by_text(qs.get("t", ""))
            else:
                self.search(qs)
```
`do_GET` 아래에 do_POST + 두 핸들러 추가:
```python
    def do_POST(self):
        length = int(self.headers.get("Content-Length", 0))
        body = self.rfile.read(length).decode("utf-8", "replace")
        qs = {k: v[0] for k, v in urllib.parse.parse_qs(body).items()}
        u = urllib.parse.urlparse(self.path)
        try:
            if u.path == "/similar_text":
                self.similar_by_text(qs.get("t", ""))
            else:
                self.send_page("<p>알 수 없는 요청</p>")
        except Exception as e:
            self.send_page(f"<p>오류: {html.escape(str(e))}</p>")

    def similar_by_clause(self, cid):
        c = self.conn()
        r = c.execute("SELECT c.text, c.title, d.member_cd FROM clauses c "
                      "JOIN documents d USING(doc_id) WHERE clause_id=?", (cid,)).fetchone()
        if not r:
            c.close(); self.send_page("<p>조문 없음</p>"); return
        rows = simmatch.db_similar(c, r["text"], self.idf, self.default_idf,
                                   top_n=15, exclude_member=self.self_member,
                                   query_title=r["title"])
        c.close()
        self.send_page(f"<p class='meta'>이 조문과 닮은 타사 조문</p>{self.render_similar(rows)}")

    def similar_by_text(self, text):
        text = (text or "").strip()
        form = ("<form method='post' action='/similar_text'>"
                "<p class='meta'>초안 조문 텍스트를 붙여넣으세요(여러 조문 가능).</p>"
                f"<textarea name='t' rows='8' style='width:100%'>{html.escape(text)}</textarea>"
                "<br><button>유사 타사 조문 찾기</button></form>")
        if not text:
            self.send_page(form); return
        from clause_split import split_clauses
        c = self.conn()
        blocks = [(no, ti, body) for no, ti, body in split_clauses(text) if (body or "").strip()]
        if not blocks:
            blocks = [(None, "", text)]
        parts = []
        for no, ti, body in blocks:
            head = f"{no or ''} {ti or ''}".strip() or "(붙여넣은 조문)"
            rows = simmatch.db_similar(c, body, self.idf, self.default_idf, top_n=10,
                                       exclude_member=self.self_member, query_title=ti)
            parts.append(f"<h2>{html.escape(head)}</h2>{self.render_similar(rows)}")
        c.close()
        self.send_page(form + "".join(parts))
```
검색결과 행(147행 근처, `조문 링크` 셀)에 "닮은 타사 조문" 링크를 덧붙임 — `view_clause`의 body(162행)에도 추가:
```python
        body = (f"<p><a href='/doc?id={r['doc_id']}'>← 이 약관의 조문 목록</a> · "
                f"<a href='/similar?id={r['clause_id']}'>🔍 닮은 타사 조문</a></p>"
```
(기존 `body = (f"<p><a href='/doc?id={r['doc_id']}'>← 이 약관의 조문 목록</a></p>"` 한 줄을 위로 교체.)
마지막으로 `main()`에서 앱 시작 시 idf 로드(205행 `App.db_path = args.db` 아래):
```python
    App.db_path = args.db
    _c = sqlite3.connect(args.db)
    try:
        App.idf, App.default_idf = simmatch.load_idf(_c)
        print(f"유사도 인덱스 로드: n-gram {len(App.idf):,}")
    except Exception as e:
        print(f"(유사도 인덱스 없음: {e} — 검색만 가능)")
    finally:
        _c.close()
```

- [ ] **Step 4: 실행해서 통과 확인**

Run: `cd ~/yakgwan-db && .venv/bin/python -m pytest tests/test_similar_render.py -v`
Expected: PASS (2 passed)

- [ ] **Step 5: 수동 연기 테스트(smoke)**

Run:
```bash
cd ~/yakgwan-db/src && ../.venv/bin/python search_app.py --db ../db/terms_dist_current.db --port 8799 &
sleep 2 && curl -s "http://127.0.0.1:8799/similar_text" | grep -c textarea ; kill %1
```
Expected: `1`(붙여넣기 폼 렌더). (조문 id 기반 `/similar?id=`는 브라우저에서 확인.)

- [ ] **Step 6: Commit**

```bash
cd ~/yakgwan-db && git add src/search_app.py tests/test_similar_render.py && \
git commit -m "feat: 단건/붙여넣기 유사 타사조문 라우트(/similar,/similar_text)"
```

---

### Task 10: `docx_split.py` + `/review` docx 일괄 심사 리포트

**Files:**
- Create: `src/docx_split.py`
- Modify: `src/search_app.py` (`/review` GET 폼 + POST multipart 처리, 의존성 가드)
- Create: `tests/test_docx_split.py`

**Interfaces:**
- Consumes: `clause_split.split_clauses`(Task 2), `simmatch.db_similar`(Task 6)
- Produces:
  - `docx_split.docx_to_text(path) -> str`
  - `docx_split.split_docx(path) -> list[tuple]` (split_clauses와 동일 형식)
  - `search_app._parse_multipart(headers, body: bytes) -> dict[str, bytes]` (파일 필드 → 바이트)

- [ ] **Step 1: 실패 테스트 작성**

Create `tests/test_docx_split.py`:
```python
import docx
from docx_split import docx_to_text, split_docx


def _make_docx(tmp_path, paras):
    d = docx.Document()
    for p in paras:
        d.add_paragraph(p)
    fp = tmp_path / "draft.docx"
    d.save(fp)
    return str(fp)


def test_docx_to_text_joins_paragraphs(tmp_path):
    fp = _make_docx(tmp_path, ["제1조 (목적)", "이 약관은 …을 정합니다."])
    t = docx_to_text(fp)
    assert "제1조" in t and "목적" in t


def test_split_docx_yields_clauses(tmp_path):
    fp = _make_docx(tmp_path, ["제1조 (목적) 이 약관은 목적을 정합니다.",
                               "제2조 (용어의 정의) 이 약관에서 쓰는 용어는 다음과 같습니다."])
    out = {no: ti for no, ti, _ in split_docx(fp) if no}
    assert out.get("제1조") == "목적"
    assert out.get("제2조") == "용어의 정의"
```

- [ ] **Step 2: 실행해서 실패 확인**

Run: `cd ~/yakgwan-db && .venv/bin/python -m pytest tests/test_docx_split.py -v`
Expected: FAIL (`ModuleNotFoundError: src.docx_split`)

- [ ] **Step 3: `docx_split.py` 구현**

Create `src/docx_split.py`:
```python
#!/usr/bin/env python3
"""초안 docx → 조문 청크. python-docx로 문단 추출 후 clause_split 재사용."""
from clause_split import split_clauses


def docx_to_text(path) -> str:
    import docx
    d = docx.Document(path)
    return "\n".join(p.text for p in d.paragraphs)


def split_docx(path):
    return split_clauses(docx_to_text(path))
```

- [ ] **Step 4: 실행해서 통과 확인**

Run: `cd ~/yakgwan-db && .venv/bin/python -m pytest tests/test_docx_split.py -v`
Expected: PASS (2 passed)

- [ ] **Step 5: `/review` 라우트 + multipart 파서 추가**

`src/search_app.py` 상단에 stdlib multipart 헬퍼 추가(예: `opts` 함수 아래):
```python
def _parse_multipart(headers, body: bytes):
    """multipart/form-data → {필드명: 값(bytes)}. cgi 미사용(3.13+ 대응)."""
    ctype = headers.get("Content-Type", "")
    if "boundary=" not in ctype:
        return {}
    boundary = ("--" + ctype.split("boundary=", 1)[1].strip()).encode()
    fields = {}
    for part in body.split(boundary):
        if not part.strip() or part.strip() == b"--":
            continue
        head, _, data = part.partition(b"\r\n\r\n")
        if not _:
            continue
        head_txt = head.decode("utf-8", "replace")
        name = None
        for tok in head_txt.split(";"):
            tok = tok.strip()
            if tok.startswith("name="):
                name = tok.split("=", 1)[1].strip().strip('"')
        if name:
            fields[name] = data.rstrip(b"\r\n")
    return fields
```
`do_GET` 라우팅에 `/review` GET(업로드 폼) 추가:
```python
            elif u.path == "/review":
                self.review_form()
```
`do_POST`에 `/review` 분기 추가(기존 if/else 사이):
```python
            if u.path == "/similar_text":
                self.similar_by_text(qs.get("t", ""))
            elif u.path == "/review":
                self.review_post()
```
> 주의: `/review`는 multipart이므로 do_POST 앞부분에서 `parse_qs`로 디코드하면 안 됨.
> do_POST를 아래처럼 분기하도록 수정(본문을 라우트별로 처리):
```python
    def do_POST(self):
        length = int(self.headers.get("Content-Length", 0))
        raw = self.rfile.read(length)
        u = urllib.parse.urlparse(self.path)
        try:
            if u.path == "/review":
                self.review_post(raw)
            elif u.path == "/similar_text":
                qs = {k: v[0] for k, v in urllib.parse.parse_qs(raw.decode("utf-8", "replace")).items()}
                self.similar_by_text(qs.get("t", ""))
            else:
                self.send_page("<p>알 수 없는 요청</p>")
        except Exception as e:
            self.send_page(f"<p>오류: {html.escape(str(e))}</p>")
```
(Task 9에서 만든 do_POST를 위 버전으로 교체.)
핸들러 두 개 추가:
```python
    def review_form(self, msg=""):
        try:
            import docx  # noqa: F401
            avail = ""
        except Exception:
            avail = "<p style='color:#933'>python-docx 미설치 — 업로드 비활성(붙여넣기는 /similar_text 사용)</p>"
        self.send_page(
            f"{avail}{msg}<form method='post' action='/review' enctype='multipart/form-data'>"
            "<p class='meta'>심사할 초안 약관(.docx)을 올리면 조문별 유사 타사 조문을 붙여줍니다.</p>"
            "<input type='file' name='f' accept='.docx'> <button>일괄 심사</button></form>")

    def review_post(self, raw):
        fields = _parse_multipart(self.headers, raw)
        blob = fields.get("f")
        if not blob or len(blob) < 100:
            self.review_form("<p>파일이 비어 있음</p>"); return
        import io
        from docx_split import docx_to_text
        try:
            text = docx_to_text(io.BytesIO(blob))
        except Exception as e:
            self.review_form(f"<p>docx 파싱 실패: {html.escape(str(e))}</p>"); return
        from clause_split import split_clauses
        blocks = [(no, ti, body) for no, ti, body in split_clauses(text)
                  if no and (body or "").strip()]
        c = self.conn()
        parts = [f"<p class='meta'>초안 조문 {len(blocks)}건 심사</p>"]
        for no, ti, body in blocks:
            rows = simmatch.db_similar(c, body, self.idf, self.default_idf, top_n=5,
                                       exclude_member=self.self_member, query_title=ti)
            head = f"{no} {ti}".strip()
            parts.append(f"<h2>{html.escape(head)}</h2>"
                         f"<pre>{html.escape(body[:400])}</pre>{self.render_similar(rows)}")
        c.close()
        self.send_page("".join(parts))
```
> `docx_to_text`는 경로/파일객체 모두 받도록 되어 있음(python-docx `Document`가 file-like 허용).
검색폼 상단(PAGE의 `<h1>` 다음)에 진입 링크 추가:
```python
<h1>약관 DB 검색 <span class="meta">__DBNAME__ · 문서 __NDOCS__건 · 조문 __NCLS__건</span></h1>
<p><a href="/review">📄 초안 docx 일괄 심사</a> · <a href="/similar_text">✍️ 조문 붙여넣기 비교</a></p>
```

- [ ] **Step 6: 통합 확인(테스트 + smoke)**

Run:
```bash
cd ~/yakgwan-db && .venv/bin/python -m pytest tests/ -v && \
cd src && ../.venv/bin/python search_app.py --db ../db/terms_dist_current.db --port 8798 &
sleep 2 && curl -s "http://127.0.0.1:8798/review" | grep -c "일괄 심사" ; kill %1
```
Expected: 전체 테스트 PASS + `1`(업로드 폼). 실제 docx 업로드는 브라우저에서 확인.

- [ ] **Step 7: Commit**

```bash
cd ~/yakgwan-db && git add src/docx_split.py src/search_app.py tests/test_docx_split.py && \
git commit -m "feat: docx 초안 일괄 심사(/review) + stdlib multipart 파서"
```

---

### Task 11: 파이프라인 편입 + 골든셋 검증 + 배포본 갱신

**Files:**
- Modify: `src/update_all.py:59-64` (build_simindex 단계 추가)
- Create: `tests/test_golden_recall.py`
- Modify: `README.md` (사용법에 유사비교·build_simindex 반영)

**Interfaces:**
- Consumes: 전 태스크 산출물.
- Produces: `update_all.py`에 `4b. 유사인덱스` 단계. top-5 재현율 측정.

- [ ] **Step 1: `update_all.py`에 build_simindex 편입**

`src/update_all.py`의 4~5단계 사이(59행 `run("4. 텍스트 추출" …)` 다음)에 추가:
```python
    ok &= run("4. 텍스트 추출", ["extract_index.py"], "extract")
    ok &= run("4b. 유사인덱스", ["build_simindex.py"], "simindex")
```

- [ ] **Step 2: 골든셋 재현율 테스트 작성**

Create `tests/test_golden_recall.py` — 반입 DB가 있을 때만 도는 통합 테스트:
```python
import os
import sqlite3

import pytest

import simmatch

DB = "db/terms_dist_current.db"
pytestmark = pytest.mark.skipif(not os.path.exists(DB), reason="반입 DB 없음")

# 초안측 쿼리(문장 살짝 변형) → 기대 상품군. top-5 안에 같은 취지 타사 조문이 들어오는지.
GOLDEN = [
    "피보험자가 보험기간 중 사망하였을 때 회사는 사망보험금을 지급합니다",
    "계약자가 보험료를 내지 아니하여 납입이 연체된 경우 회사는 납입최고를 하고 계약을 해지할 수 있습니다",
    "청약을 한 계약자는 보험증권을 받은 날부터 15일 이내에 청약을 철회할 수 있습니다",
]


def test_golden_queries_return_hits():
    c = sqlite3.connect(DB); c.row_factory = sqlite3.Row
    idf, d = simmatch.load_idf(c)
    hit = 0
    for q in GOLDEN:
        rows = simmatch.db_similar(c, q, idf, d, top_n=5, exclude_member="L34")
        if rows and rows[0]["score"] > 0.1:
            hit += 1
    c.close()
    assert hit == len(GOLDEN), f"{hit}/{len(GOLDEN)}만 히트 — 엔진/인덱스 점검"
```

- [ ] **Step 3: 골든셋 실행(검증 게이트)**

Run: `cd ~/yakgwan-db && .venv/bin/python -m pytest tests/test_golden_recall.py -v`
Expected: PASS. 실패 시 상위 결과를 눈으로 확인(아래)하고 `fts_query` term 수·`cand_limit`·`df_floor`를 조정:
```bash
cd ~/yakgwan-db && .venv/bin/python - <<'PY'
import sys, sqlite3; sys.path.insert(0, "src"); import simmatch
c=sqlite3.connect("db/terms_dist_current.db"); c.row_factory=sqlite3.Row
idf,d=simmatch.load_idf(c)
for r in simmatch.db_similar(c,"피보험자가 사망하면 사망보험금을 지급합니다",idf,d,top_n=5,exclude_member="L34"):
    print(round(r["score"],3), r["insurer"], r["clause_no"], r["title"])
PY
```

- [ ] **Step 4: README 갱신**

`README.md`의 "사용법" 코드블록(36~55행)에 두 줄 추가:
```bash
.venv/bin/python src/build_simindex.py               # ④b 문자 n-gram IDF 적재(유사비교용)
```
그리고 search_app 설명줄(53~54행)에 유사비교 기능 한 줄 추가:
```
# 유사비교: 조문 열람 화면의 "닮은 타사 조문", /similar_text(붙여넣기), /review(docx 일괄 심사)
```

- [ ] **Step 5: 전체 테스트 + Commit**

Run: `cd ~/yakgwan-db && .venv/bin/python -m pytest tests/ -v`
Expected: 전체 PASS.
```bash
cd ~/yakgwan-db && git add src/update_all.py tests/test_golden_recall.py README.md && \
git commit -m "feat: update_all에 유사인덱스 편입 + 골든셋 재현율 검증 + README"
```

---

## 완료 기준 (전체 검증)

- [ ] `.venv/bin/python -m pytest tests/ -v` 전량 PASS.
- [ ] 신한·KB·NH `제N조` 제목 공란율이 개선 전 대비 뚜렷이 하락(Task 3 게이트).
- [ ] 반입 DB(`terms_dist_current.db`)에 `ngram_idf` 포함, 크기 증가 설계 목표 이내(Task 7 게이트).
- [ ] 브라우저에서 (a) 조문 열람 → "닮은 타사 조문", (b) `/similar_text` 붙여넣기, (c) `/review` docx 업로드가 모두 회사별 유사조문을 반환.
- [ ] 골든셋 3개 쿼리가 top-5 히트(Task 11 게이트).
- [ ] 배포본 재생성: `_dist/약관검색_v1` 갱신(별도 배포 스크립트가 있으면 재실행; 본 계획 범위 밖이면 수동).

## 리스크·후속

- 흥국 등 PDF 추출 뒤엉킴 문서는 제목 복구가 부분적 — 재현율 낮으면 해당 사만 별도 추출 튜닝(후속).
- IDF vocab이 커 반입 크기 목표 초과 시 `--df-floor` 상향(재현율과 트레이드오프, 골든셋으로 확인).
- Phase 2(표준약관 적재)·Phase 3(감독규정 연결)은 별도 spec/plan.
